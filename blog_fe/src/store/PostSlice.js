import { createSlice } from "@reduxjs/toolkit";
const PostSlice = createSlice({
    name : "post",
   initialState : {
     value : "test"
   },
   reducers : {

   },

});
export default PostSlice.reducer;